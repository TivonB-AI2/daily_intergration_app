/**
 * Application-wide branding constants.
 *
 * Change `APP_NAME` here and it will propagate to the sidebar, sign-in page,
 * page titles, and anywhere else it is referenced.
 */

/** Primary display name shown in the sidebar header, sign-in branding, etc. */
export const APP_NAME = "Platform";

/** Short descriptor shown beneath the app name (e.g. "Enterprise"). */
export const APP_DESCRIPTION = "Enterprise";
